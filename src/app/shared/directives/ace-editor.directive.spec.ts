import { AceEditorDirective } from './ace-editor.directive';

describe('AceEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new AceEditorDirective();
    expect(directive).toBeTruthy();
  });
});
