import { AlertSearchDirective } from './ace-editor.directive';

describe('AlertSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertSearchDirective();
    expect(directive).toBeTruthy();
  });
});
